
export interface CountdownTime {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}

export interface Feature {
  title: string;
  description: string;
  details: string;
  icon: string;
}

export interface SocialLink {
  icon: string;
  url: string;
  label: string;
}
